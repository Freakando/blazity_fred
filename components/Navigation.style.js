import styled from "styled-components";

export const NawSection = styled.div`
  position: sticky;
  top: 0;
  z-index: 100;
  transition-property: transform, visibility, height;
  transition-duration: 0.2s;
  transition-timing-function: ease-in-out;
`;
